# Dependencies are now managed via pyproject.toml + uv.
# To install: uv sync
# For dev deps: uv sync --extra dev
#
# This file is kept for compatibility with tools that expect requirements.txt.
-e .
